[project]
name = "constitution"
version = "0.1.0"
description = "constitution"
authors = [
    {name = "Andrew Mpapalika"}
]
license = {text = "Apache 2.0"}
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
