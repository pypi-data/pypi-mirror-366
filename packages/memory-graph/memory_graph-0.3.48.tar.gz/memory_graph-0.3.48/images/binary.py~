import memory_graph as mg

def binary(value: int) -> list[int]:
    mg.block(mg.show, mg.stack())
    if value == 0:
        return []
    quotient, remainder = divmod(value, 2)
    result = [remainder] + binary(quotient)
    mg.block(mg.show, mg.stack())
    return result

print( binary(66) )
