#
# Copyright (c) 2019, 2025, firmer.tech and/or its affiliates. All rights reserved.
# Firmer Corporation PROPRIETARY/CONFIDENTIAL. Use is subject to license terms.
#
#

# Code generated by mesh; DO NOT EDIT.

from abc import ABC, abstractmethod
from mesh.macro import mpi, spi
from mesh.types import Page, Paging, Script
from typing import Dict, List

@spi("mpi")
class Evaluator(ABC):

    @abstractmethod
    @mpi("mesh.eval.compile")
    async def compile(self, script: Script) -> str:
        """
        Compile the named rule.
        """
        pass

    @abstractmethod
    @mpi("mesh.eval.exec")
    async def exec(self, code: str, args: Dict[str, str], dft: str) -> str:
        """
        Exec the script with name.
        """
        pass

    @abstractmethod
    @mpi("mesh.eval.dump")
    async def dump(self, feature: Dict[str, str]) -> List[Script]:
        """
        Dump the scripts.
        """
        pass

    @abstractmethod
    @mpi("mesh.eval.index")
    async def index(self, index: Paging) -> Page[Script]:
        """
        Index the scripts.
        """
        pass
