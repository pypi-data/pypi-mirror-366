[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "td_mion_orbitrap"
version = "0.4.0"
description = "CLI pipeline for TD‑MION-Orbitrap thermograms, blanks, KMD"
authors = [{name = "Mihai Ciobanu"}]
requires-python = ">=3.10"
dependencies = [
  "click>=8",
  "pymzml>=2.5.11",
  "pandas>=2",
  "numpy>=1.26",
  "matplotlib>=3.9",
  "pyyaml>=6",
  "pyarrow>=16.0.0"
]

[project.scripts]
td-mion = "td_mion_orbitrap.cli:cli"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]