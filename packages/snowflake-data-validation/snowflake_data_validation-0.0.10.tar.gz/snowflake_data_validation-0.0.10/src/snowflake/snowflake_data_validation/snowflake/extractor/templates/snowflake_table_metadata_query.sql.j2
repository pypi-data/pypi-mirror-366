WITH rc_cte AS (
    SELECT COUNT(*) AS ROW_COUNT FROM {{ fully_qualified_name }} {% if has_where_clause %} WHERE {{ where_clause }} {% endif %} LIMIT 1
)

SELECT
    C.TABLE_NAME AS TABLE_NAME,
    C.COLUMN_NAME as COLUMN_VALIDATED,
    C.ORDINAL_POSITION AS ORDINAL_POSITION,
    C.DATA_TYPE AS DATA_TYPE,
    C.CHARACTER_MAXIMUM_LENGTH AS CHARACTER_MAXIMUM_LENGTH,
    C.NUMERIC_PRECISION AS NUMERIC_PRECISION,
    C.NUMERIC_SCALE AS NUMERIC_SCALE,
    C.IS_NULLABLE AS IS_NULLABLE,
    rc.ROW_COUNT
FROM INFORMATION_SCHEMA.COLUMNS C
CROSS JOIN rc_cte rc
WHERE C.TABLE_NAME = '{{ object_name|upper }}'
AND C.TABLE_SCHEMA = '{{ object_schema|upper }}'
{% if columns %}
    AND C.COLUMN_NAME IN (
        {% for column in columns %}
            '{{ column|upper }}'{% if not loop.last %}, {% endif %}
        {% endfor %}
    )
{% endif %}
