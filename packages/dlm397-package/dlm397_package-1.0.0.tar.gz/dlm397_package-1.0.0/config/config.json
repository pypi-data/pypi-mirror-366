{
    "paths": {
        "data_dir": "storage\\data",
        "storage_dir": "storage",
        "log_file": "logs\\app.log"
    },
    "settings": {
        "default_format": "csv",
        "max_file_size_mb": 100
    }
}