[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0", "wheel"]

[project]
name = "toolsman"
description = "Python Useful Tools"
version = "0.0.3"
requires-python = ">=3.10"

authors = [{ name = "wauo" }]
maintainers = [{ name = "wauo", email = "markadc@126.com" }]

dependencies = ["loguru"]

license = "MIT"
readme = "README.md"
# urls = {Homepage = "https://github.com/markadc/coocan"}

[tool.setuptools]
include-package-data = true
packages = { find = {} }
