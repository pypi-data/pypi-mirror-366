[project]
name = "ayleea"
version = "0.2.2"
description = "An Std Json Tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ['pandas','ujson','tqdm','opencv-python']

#[build-system]
#requires = ["uv-build"]
#build-backend = "uv-build"