[project]
name = "simplecalculator-mike-tyson"
version = "0.1.0"
description = "A simple arithmetic calculator in Python"
authors = [{ name="Mike" }]
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
