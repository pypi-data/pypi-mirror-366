[project]
name = "promptdir"
version = "0.6.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pbeagan" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
promptdir = "promptdir:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

