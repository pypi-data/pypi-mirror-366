[project]
name = "format_seismo"
version = "0.1.0"
description = "A Python package to format seismic data"
authors = [{ name = "Domagoj Hrvojevic", email = "domagoj@example.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ['obspy', 'numpy']

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"