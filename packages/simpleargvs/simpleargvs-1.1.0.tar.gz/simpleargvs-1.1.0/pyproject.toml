[project]
name = "simpleargvs"  # must be unique on PyPI
version = "1.1.0"
description = "A simpler way to use sys.argvs"
authors = [
  { name="Bob Robertson", email="Coderdude123@proton.me" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"
dependencies = []

[project.urls]
Homepage = "https://github.com/Jeff-web-sketch/argvs-lib-for-python/tree/main/argvs"


[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
