[build-system]
requires = ["setuptools"]

[project]
name = "pycupra"
version = "0.1.12"
requires-python = ">=3.10"
dependencies = [
    "aiohttp",
    "beautifulsoup4",
    "cryptography",
    "lxml",
    "PyJWT",
    "xmltodict",
    "pandas"
]