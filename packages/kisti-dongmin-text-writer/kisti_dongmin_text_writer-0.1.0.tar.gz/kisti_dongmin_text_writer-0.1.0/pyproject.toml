[project]
name = "kisti-dongmin-text-writer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "build>=1.3.0",
    "mcp[cli]>=1.12.3",
    "twine>=6.1.0",
]

[project.scripts]
kisti-dongmin-text-writer = "kisti_dongmin_text_writer:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
