[project]
name = "pyekv"
version = "0.1.1"
description = "Simple, SQLite-backed KV storage. Best suited for prototypes, tools, or local scripts."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
