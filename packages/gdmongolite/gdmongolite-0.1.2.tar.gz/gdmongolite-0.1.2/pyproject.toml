[tool.poetry]
name = "gdmongolite"
version = "0.1.2"
description = "gdmongolite is a zero-boilerplate, schema-first, multi-driver MongoDB toolkit that unifies synchronous and asynchronous drivers, Pydantic-powered validation, automatic migrations, telemetry hooks, and a full CLI—all in one package. You write only your data models and queries; gdmongolite handles the rest."
authors = ["Ganesh Datta Padamata <ganeshdattapadamata@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "gdmongolite", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.8, <4.0"
click = "^8.0"
motor = "^3.3.2"
python-dotenv = "^1.0.0"
pydantic = {version = "^2.5.3", extras = ["email"]}
aioconsole = "^0.6.1"


[tool.poetry.group.dev.dependencies]
pytest = ">=7.0"
black = "^23.0"
ruff = "^0.1"

[tool.poetry.scripts]
gdmongolite = "gdmongolite.cli:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
