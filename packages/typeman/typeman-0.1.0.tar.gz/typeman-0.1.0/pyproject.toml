[project]
name = "typeman"
version = "0.1.0"
description = "A terminal‑based typing speed test CLI tool (WPM & accuracy)."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
dependencies = [
    "readchar>=4.2.1",
    "ruff>=0.12.2",
    "typer>=0.16.0",
]

[project.scripts]
typeman = "typeman.main:app"

[tool.setuptools.package-data]
"typeman.assets" = ["common_words.txt"]
