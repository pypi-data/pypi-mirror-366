[pytest]
addopts = -m"not gamdpy_cpu" --doctest-modules gamdpy tests --verbose --durations=0
markers = 
	slow: marks test as slow (deselect with '-m "not slow"')
	experimental: marks features not fully released (deselect with '-m "not experimental"')
	gamdpy_cpu: cpu version of gamdpy using gpu emulator
