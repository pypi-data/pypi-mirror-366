[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "md-to-slack-blocks"
version = "0.1.0"
description = "A simple python library to convert markdown to the Slack blocks"
readme = "readme.md"
license = "MIT"
authors = [{name = "Karthikeya Meesala", email = "mkarthikeya41@gmail.com"}]
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"
