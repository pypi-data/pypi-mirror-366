[project]
name = "emota-loader"
version = "2.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.12.7",
]

[tool.ruff]
line-length = 88
target-version = "py310"
lint.select = ["E", "F"]
lint.ignore = ["E501"]

[tool.setuptools.packages.find]
where = ["."]
include = ["emota_loader*"]