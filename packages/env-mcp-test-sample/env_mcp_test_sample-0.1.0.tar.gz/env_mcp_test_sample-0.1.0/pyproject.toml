[project]
name = "env-mcp-test-sample"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "build>=1.2.2.post1",
    "mcp[cli]>=1.12.2",
    "requests>=2.32.4",
    "twine>=6.1.0",
]

[project.scripts]
env-mcp-test-sample = "env_mcp_test_sample.env_mcp_test_sample:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/env_mcp_test_sample"]
