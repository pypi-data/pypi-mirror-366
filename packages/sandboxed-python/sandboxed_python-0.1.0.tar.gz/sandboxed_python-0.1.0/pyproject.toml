[project]
name = "sandboxed-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "thautwarm", email = "twshere@outlook.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
sandboxed-python = "sandboxed_python:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-cov>=6.2.1",
]
