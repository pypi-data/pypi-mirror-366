TOMCLI-FORMATTERS(1)

# NAME

tomcli-formatters — CLI for working with TOML files. Pronounced "tom clee."

# SYNOPSIS

*\[tomcli-formatters \| tomcli formatters]* [OPTIONS]

# DESCRIPTION

The *tomcli-formatters* subcommand lists formatters available for use with
tomcli-get.

# OPTIONS

*--version*
	Show the version and exit.
*--builtin-only*
	List only builtin formatters and exclude formatters provided by plugins.

# SEE ALSO

tomcli(1), tomcli-get(1), tomcli-set(1)
