# 运行环境设置 (dev/staging/prod)
HOMELAB_ENVIRONMENT=dev

# Consul 服务器地址列表，支持多个地址，用逗号分隔
# 例如：本地开发环境、Kubernetes 集群内部服务、外部域名
HOMELAB_CONSUL_URLS=http://localhost:8500,https://consul.default.svc.cluster.local:8500,https://consul.example.com

# Consul 认证令牌 (可选)
# 用于访问需要认证的 Consul 服务
HOMELAB_CONSUL_TOKEN=your-consul-token-here

# 连接缓存时间 (秒)
# 默认值：300 秒
HOMELAB_CONSUL_CACHE_TTL=300
